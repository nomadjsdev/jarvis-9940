import styled from 'styled-components'

import { spaces } from 'Styles'

export const MainDiv = styled.div`
	min-height: 100vh;
`

export const Container = styled.div`
	max-width: 1024px;
	margin: 0 auto;
	padding: ${spaces.gutterPadding};
`
