import React from 'react'

const Create = () => {
	return (
		<>
			<h1>Create</h1>
			{/* TODO:
				Create pulldown menus from available games and activities
				Should session ID creation and checking be done here?
				After creation, should this forward through /join or perform join dispatches in create functions?
			*/}
		</>
	)
}

export default Create
